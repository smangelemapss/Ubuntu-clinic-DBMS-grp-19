# Backend API base URL (no trailing slash). Must match backend FLASK_PORT (default 8000).
# For production: set to your deployed API origin, then run `npm run build`.
VITE_API_URL=http://localhost:8000

# Dev-only: set true to use empty mock data without Oracle. Keep false for demos/exams.
VITE_USE_MOCK=false
